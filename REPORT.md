# 조선 야행 — 산속 미로 탈출 (컴퓨터그래픽스 기말 프로젝트)

> Three.js 기반 1인칭 공포 미로 게임. 핵심 그래픽스 기술로 **DDGI(Dynamic Diffuse Global Illumination)** 를 직접 구현한다.
>
> **플레이 링크:** _(배포 후 기입)_ · **소스:** _(git 링크 기입)_

> ⚠️ 작성 규칙(채점 기준): 모든 항목은 **본 게임에서 직접 캡쳐한 이미지**로 설명한다. 외부/예시 이미지로 설명한 항목은 0점 처리됨. 아래 `[캡쳐: ...]` 자리는 실제 플레이 캡쳐로 교체할 것.

---

## 1. 게임 개요 (기획)

- **컨셉:** 칠흑 같은 조선 산속, 등불 하나에 의지해 미로를 빠져나가는 공포 탈출 게임.
- **조작:** `WASD` 이동 · 마우스 시점 · `Shift` 달리기.
- **목표:** 출구 셀에 도달하면 탈출 성공.
- **왜 미로 + 등불인가 (GI 관점):** 어두운 실내 + 강한 색의 벽(단청 적색) + 이동하는 광원(등불) 조합은 **간접광·색 번짐(color bleeding)** 이 가장 잘 드러나는 무대다. DDGI의 효과를 시각적으로 증명하기에 최적.

`[캡쳐: 타이틀 화면]`
`[캡쳐: 인게임 1인칭 플레이 화면]`

---

## 2. 강의 내용 ↔ 구현 매핑

> 강의에서 다룬 그래픽스 파이프라인(좌표 변환 → 셰이딩/라이팅 → 텍스처 → 글로벌 일루미네이션 → 애니메이션) 순서로 매핑한다.

### 2.1 좌표 변환 파이프라인 (Model → World → View → Projection)
- 미로 벽은 로컬 박스 지오메트리(Model space)를 `InstancedMesh`의 인스턴스 행렬로 World space에 배치 → 강의의 **SRT/Model 변환** 대응.
- 1인칭 카메라가 View 변환을, `PerspectiveCamera`가 Projection 변환을 담당.
- `[캡쳐: 미로 전체 평면(top-down) — 인스턴싱된 월드 배치]`

### 2.2 셰이딩 & 라이팅 (Phong / Blinn-Phong, 광원 타입)
- `MeshStandardMaterial`(물리 기반) + 점광원(등불, `PointLight`)으로 **확산광/반사광/거리 감쇠** 표현 → 강의의 폼 반사 모델·Attenuation 대응.
- 광원 타입: 등불=Point Light, 달빛=Directional Light → 강의 Light Source Types 대응.
- `[캡쳐: 등불 근처 벽의 하이라이트/감쇠]`

### 2.3 텍스처 (예정)
- 벽/바닥에 단청·흙 텍스처 + UV 매핑 적용 예정 → 강의 Texturing & UV/Texel 대응.
- `[캡쳐: 텍스처 적용 전/후 비교]`

### 2.4 글로벌 일루미네이션 — DDGI (핵심, 예정)
- 프로브 격자 + 옥타헤드럴 irradiance atlas + SDF 레이마칭 + Chebyshev 가시성으로 **DDGI 직접 구현**.
- `[캡쳐: GI OFF vs GI ON 비교 — 간접광/색 번짐]`
- `[캡쳐: 등불 이동 시 간접광 실시간 변화(Dynamic)]`
- 상세는 §4.

### 2.5 애니메이션 (예정)
- 몬스터(호랑이/귀신) 이동·추적, 등불 흔들림(flicker) → 강의 애니메이션/오일러·쿼터니언 대응.
- `[캡쳐: 몬스터 추적 장면]`

---

## 3. 개발 상세

### 3.1 미로 생성 — 재귀 백트래커 + 점유 격자
- `cellsX × cellsZ` 셀 격자에 재귀 백트래커로 완전 미로를 생성하고, `(2W+1)×(2H+1)` **점유 격자(1=벽, 0=바닥)** 로 확장.
- 이 점유 격자는 **충돌·렌더링·DDGI의 SDF가 공유하는 단일 진실 공급원(single source of truth)**.
- `[캡쳐: 생성된 미로 평면도]`

### 3.2 충돌 처리
- 플레이어를 원(disc)으로 보고 점유 격자의 벽 셀 AABB와 교차 검사. X·Z축 분리 이동으로 벽을 따라 미끄러짐.

### 3.3 등불 (이동 광원)
- 카메라를 따라다니는 따뜻한 점광원 + flicker. 그림자 맵 포함.

---

## 4. DDGI 구현 상세 (작성 예정)

- 4.1 프로브 그리드 배치
- 4.2 SDF 레이마칭으로 프로브 광선 추적 (하드웨어 RT 없이 WebGL2에서)
- 4.3 옥타헤드럴 irradiance/depth atlas
- 4.4 Chebyshev 가시성 테스트 (빛 샘 방지) — "irradiance probe"와 "DDGI"를 가르는 지점
- 4.5 동적 갱신
- 각 절마다 `[캡쳐]` 필수.

---

## 5. 실행 및 배포

```bash
npm install
npm run dev      # 로컬 개발 (http://localhost:5180)
npm run build    # 정적 빌드 → dist/
npm run preview  # 빌드 결과 미리보기
```

- **배포:** _(GitHub Pages / Vercel / Netlify 중 택1 — 링크 기입)_
